# RetroSurround
A VST3 plugin that decodes matrix-encoded stereo (Dolby Surround / Pro Logic / Pro Logic II style content) into discrete multichannel audio in real time — for using an always-on surround decoder system-wide (e.g. via Equalizer APO VS3 Fork) rather than only inside a DAW.
